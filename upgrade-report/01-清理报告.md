# 01 项目体检与垃圾文件清理报告

> 生成时间：2026-08-04
> 项目路径：`C:\Users\冷漠不是神\Desktop\短视频去水印\video_spider-main`
> 说明：本报告只做体检与清理清单输出，**删除类操作尚未执行**，待用户确认后另行处理。

## 1. 技术栈识别

| 类别 | 识别结果 |
| --- | --- |
| 前端框架 | 无（原生 HTML5 + CSS3 + JavaScript，单文件 `public/app.php`） |
| 后端框架 | 无（原生 PHP 8.1+，当前运行 PHP 8.3.33） |
| 构建工具 | 无前端构建；PHP 侧无编译步骤 |
| 包管理器 | Composer（`composer.json` + `composer.lock`）；无 npm/yarn/pnpm |
| 语言 | PHP（后端/网页）+ JavaScript（抖音 `a_bogus` 签名脚本，Node 18+，当前 Node 24.18.0） |
| 数据库 / ORM | 无数据库；文件存储（`storage/logs`、`storage/rate_limit`） |
| CSS 方案 | 内联 CSS 变量（已具备基础 Token：`--bg`、`--surface`、`--accent` 等） |
| 第三方依赖 | `vlucas/phpdotenv` v5.6.4 及 polyfill 包 |

## 2. 项目体积与目录分布

| 范围 | 文件数 | 体积 |
| --- | ---: | ---: |
| 项目总计 | 约 42（含 vendor） | 0.49 MB |
| 非 vendor 业务代码 | 30 | 0.16 MB |
| vendor 依赖 | 12 | 0.33 MB |
| `storage/logs/app.log` | 1 | 0.25 KB |
| `storage/rate_limit/*.json` | 1 | 0.03 KB |

主要代码文件：

| 文件 | 体积 | 说明 |
| --- | ---: | --- |
| `public/app.php` | 39.5 KB | 网页版界面（HTML/CSS/JS 单文件） |
| `scripts/a_bogus.js` | 19.9 KB | 抖音签名脚本 |
| `app/Parsers/DouyinParser.php` | 9.9 KB | 抖音解析器 |
| `app/Services/MediaProxy.php` | 5.6 KB | 服务端媒体代理 |
| `app/Utils/HttpClient.php` | 6.2 KB | HTTP 客户端 |

## 3. 垃圾与冗余文件扫描结果

| 类别 | 扫描结果 | 预估释放 |
| --- | --- | ---: |
| 历史构建产物（dist/build/.next 等） | 未发现 | 0 |
| 过期依赖缓存（node_modules/__pycache__ 等） | 未发现；`vendor/` 与 `composer.lock` 一致，应保留 | 0 |
| 版本残留文件（old/backup/bak/v1/_copy/(1) 等） | 未发现 | 0 |
| 临时/日志文件（*.log/*.tmp 等） | `storage/logs/app.log`（2 条微博 cookie 错误日志） | 约 0.25 KB |
| IDE 个人配置残留（.idea/.vscode 等） | 未发现 | 0 |
| MD5 完全相同的重复文件 | 未发现 | 0 |
| 空目录 | 未发现 | 0 |
| 废弃配置文件（tslint/旧 babelrc 等） | 未发现 | 0 |
| 未引用静态资源 | 无独立静态资源目录；favicon 为内联 SVG | 0 |
| Git 忽略但仍被追踪的文件 | 项目无 `.git` 仓库；`.gitignore` 已覆盖 vendor、日志、速率限制数据 | 0 |

## 4. 待确认清理清单

| 编号 | 操作 | 影响 | 预估释放 |
| --- | --- | --- | ---: |
| A | 清空 `storage/logs/app.log` | 仅删除 2 条历史错误日志 | 0.25 KB |
| B | 清理 `storage/rate_limit/*.json` | 重置当前 IP 的速率限制计数（服务会自动重建） | 0.03 KB |
| C | 保留 `vendor/` | 删除后需重新执行 `composer install` | 0 |

**清理前后对比**：清理前 0.49 MB，清理后约 0.49 MB。项目本身非常精简，垃圾文件占比可忽略，主要收益是日志与限流状态归零，而不是磁盘空间。

## 5. 建议

1. 日志加入按大小轮转策略（例如超过 1 MB 自动归档），避免长期运行后无限增长。
2. 速率限制文件改为目录级保留策略，防止频繁写入产生碎片。
3. 若引入前端构建，再单独处理 `node_modules`、`dist` 等产物目录。
